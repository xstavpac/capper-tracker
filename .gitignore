# dependencies
node_modules/

# next.js build output
.next/
out/

# environment variables - never commit real secrets
.env
.env*.local

# prisma
prisma/*.db

# misc
.DS_Store
*.pem
npm-debug.log*
tsconfig.tsbuildinfo

.vercel

# Supabase CLI local state (project link cache, etc.) - not app code
supabase/.temp/

.vercel
.env*

# T2 harness snapshot dumps - real (anonymized or not) database dumps must
# never be committed, only referenced by local path. See prod-guard.mjs /
# extract-from-dump.mjs for how these are produced and consumed locally.
scripts/t2-harness/.snapshots/
*.dump
capper_tracker_7table.dump
